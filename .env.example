ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# Project Learning / Document Dictionary V1 (validated schedule/legend context).
# When set to 1, Stage 5 harvests schedule/notes/index pages (ODL local or Hybrid),
# interprets regions, validates definitions, and injects knownDefinitions into
# physical Stage 6 extraction context. Off = current behavior.
# Hybrid (textless schedules) needs Java 11+ and optional local opendataloader-pdf
# hybrid server; unavailable Hybrid falls back without failing the takeoff.
TAKEOFF_PROJECT_LEARNING=
